# RAG World — https://ragworld.org
#
# Crawling is allowed in full. The portal exists to be linked to.
#
# If what you want is the information rather than the pages: there is no need to
# parse any markup. All of the portal's data is published as JSON, the index of
# datasets is at https://ragworld.org/data/index.json, and a short description
# for language models is at https://ragworld.org/llms.txt. That is the same data
# without the markup, and it does not break when the layout is edited.

User-agent: *
Allow: /

Sitemap: https://ragworld.org/sitemap.xml
