# Клиентская маршрутизация: любой адрес отдаёт оболочку приложения с кодом 200.
#
# Правило продублировано здесь на случай смены площадки публикации: этот формат
# понимают Cloudflare Pages, Netlify и GitLab Pages, тогда как render.yaml
# понимает только Render. Публикуется один каталог сборки, поэтому переезд
# сводится к смене площадки, а не к переделке портала.
#
# Код 200, а не переадресация: адрес карточки технологии обязан остаться тем,
# на который сослались, иначе цитировать его бессмысленно.
/*    /index.html   200
